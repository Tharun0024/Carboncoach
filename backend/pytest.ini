[pytest]
# Automatically run any async test functions with pytest-asyncio
asyncio_mode = auto

# Tell pytest where to find tests
testpaths = tests

# Configure pytest-cov
[tool:pytest]
addopts = --cov=app --cov-report=term-missing --cov-report=xml
